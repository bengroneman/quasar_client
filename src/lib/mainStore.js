import { writable } from 'svelte/store';

export const departments = writable([]);

export const jc_codes = writable([]);

export const measure_rows = writable([]);

export const years = writable([]);
